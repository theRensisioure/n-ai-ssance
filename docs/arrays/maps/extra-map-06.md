# MAP · scan-sessions skill teaching path

## Unit path
docs/arrays/maps/extra-map-06.md

## Map
- **Skill name:** scan-sessions (`/scan-sessions` when D4 ships)
- **What this is:** Docs-only **teaching path** for inventory of session roots without a board server
- **Not this:** Running `:8765`, WebView host, full cross-provider perfection before Grok path works
- **Intent spine:** Finding work is inventory, not nostalgia for a GUI — path, project, meaning-first title; optional filters
- **Synthetic ground:** Alex on a laptop without desktop suite; needs which Grok sessions touched example-app this week
- **Doc chain (ordered):**
  1. PRODUCT.md row for scan-sessions
  2. docs/use-cases/04-scan-sessions.md
  3. docs/socratic/SERIES-04-scan.md (P1–P5, done-when)
  4. Title law shared with session-map (meaning-first, not id slice)
  5. docs/arrays unit maps for scan-sessions × audiences
  6. prompt pack prompt-scan-sessions-01…08 when drilling filter and list shape
- **Learner outcomes:**
  - Would ship scan-sessions even if suite never opens on the machine
  - Can demand a first filter when twenty hits return (project / agent / age)
  - Bans id-sliced titles in one sentence of policy
  - Chooses primary output shape (CLI list vs agent narration) without waiting on UI
- **Ring law:** Pure agent/CLI inventory; no board server dependency in the teaching story

## Keep
- Sterile titles and fake roots only (example-app, synthetic session names)
- Pattern paths for roots (`~/.grok/sessions/`, provider-agnostic placeholders)
- Meaning-first title as the findability surface
- Filter story before “index everything forever”
- Link D4 shipping order; honesty that skill body may not exist yet

## Toss
- Live session directory trees or real sids
- Absolute user-home dumps as “examples”
- Suite launch as a prerequisite for the lesson
- Cross-provider perfection before Grok path is teachable
- Implementing skills/scan-sessions/ in this map’s job (docs path only)
- Treating “I can’t find it” only as tooling when it is re-entry avoidance (name both)

## Smoke test
1. File exists under docs/arrays/maps/ with this name
2. Teaching path names scan-sessions and no-server inventory
3. No absolute user-home paths; no secrets; no live session trees
4. Title law and at least one filter dimension appear
5. Done-when aligns with SERIES-04 (ship without suite)
6. `python scripts/sterile-check.py` still green after add

## Next commit
- Align with D4 SKILL.md when it lands (roots, filters, output shape)
- Keep use-case 04 + SERIES-04 as story spine
- Optional sterile five-title readability ranking exercise in fixtures or FOR-READERS
- Park multi-provider completeness until one provider path is solid

## Salience
High for pure CLI/agent environments; Instant for pack v1 teaching of findability. Secondary to session-map for handoff, primary for “which session exists at all.”

## Version role
Extra map · skill teaching path (scan-sessions). Complements unit-scan-sessions__* audience maps. Prep for D4; finder without suite host.
